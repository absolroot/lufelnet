;(function () {
  if (typeof window === 'undefined') return;

  window.NaviIcons = window.NaviIcons || {};
  window.NaviIcons['후타바'] = true;

  // EN now uses codename_en directly, so no extra DOM override is needed.
  window.applyNaviCodenameOverride = function applyNaviCodenameOverride() {};
})();
